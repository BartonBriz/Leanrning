#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    int OddNum = 0;
    for (int i = 0; i < 10; i++)
    {
        if (i % 2 == 1)
        {
            cout << i << " = " << pow(i,2) << endl;
        }
    }
    return 0;
}
